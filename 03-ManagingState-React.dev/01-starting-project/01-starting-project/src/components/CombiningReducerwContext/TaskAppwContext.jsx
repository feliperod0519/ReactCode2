import { createContext } from "react";

export const TasksContextwContext = createContext(null);
export const TasksDispatchContextwContext = createContext(null);

export default function TaskAppWContext(){

}